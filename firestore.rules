rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // Aerion uses PostgreSQL as its primary database.
    // Firestore access is denied by default.
    // Add per-collection rules here only if Firestore is needed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
